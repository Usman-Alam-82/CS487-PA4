apiVersion: apps/v1
kind: Deployment
metadata:
  name: validate-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: validate-api
  template:
    metadata:
      labels:
        app: validate-api
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: validate-api
        image: pa426100383.azurecr.io/validator-api:v1
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "250m"
            memory: "500Mi"
